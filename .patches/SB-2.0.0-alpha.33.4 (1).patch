diff --git a/README.md b/README.md
index cc5a7b0..dd7ad3e 100644
--- a/README.md
+++ b/README.md
@@ -1,4 +1,11 @@
-## SB-2.0.0-alpha.33.3.3
+## SB-2.0.0-alpha.33.4
+
+### Alpha 33.4 — contraste automatique des légendes
+
+- Ajout automatique d’un filet gris autour des symboles blancs ou très clairs lorsque leur contour est lui-même trop clair.
+- Le contraste est appliqué aux surfaces, cercles, symboles et lignes sans modifier la couleur cartographique enregistrée.
+- Même comportement dans l’éditeur, l’aperçu et l’export scrollytelling.
+
 
 - Correctif : les aperçus du sélecteur commun de calques utilisent désormais les styles du chapitre actif.
 - Le sélecteur se rafraîchit automatiquement lorsqu’un style, le chapitre sélectionné ou le projet change.
diff --git a/index.html b/index.html
index a326dd5..c72bb15 100644
--- a/index.html
+++ b/index.html
@@ -3,7 +3,7 @@
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
-    <title>Story Builder — 2.0.0-alpha.33.3.3</title>
+    <title>Story Builder — 2.0.0-alpha.33.4</title>
     <link href="https://api.mapbox.com/mapbox-gl-js/v3.25.0/mapbox-gl.css" rel="stylesheet">
     <script src="https://api.mapbox.com/mapbox-gl-js/v3.25.0/mapbox-gl.js"></script>
     <link rel="stylesheet" href="style.css">
diff --git a/js/core/config.js b/js/core/config.js
index 96f78ee..332836c 100644
--- a/js/core/config.js
+++ b/js/core/config.js
@@ -1,4 +1,4 @@
-export const APP_VERSION = "2.0.0-alpha.33.3.3";
+export const APP_VERSION = "2.0.0-alpha.33.4";
 export const PROJECT_FORMAT = "story-builder-project";
 export const PROJECT_FORMAT_VERSION = 1;
 export const STORAGE_KEY = "storyBuilderProjectV2Architecture1";
diff --git a/js/features/legend/legend-symbol.js b/js/features/legend/legend-symbol.js
index e419d95..5ebf942 100644
--- a/js/features/legend/legend-symbol.js
+++ b/js/features/legend/legend-symbol.js
@@ -42,9 +42,21 @@ export function createSymbolPreview(symbol = {}) {
     preview.style.setProperty("--legend-outline", symbol.outlineColor || symbol.color || "#4b78ff");
     preview.style.setProperty("--legend-opacity", String(symbol.opacity ?? 1));
     preview.style.setProperty("--legend-width", `${Math.max(1, Math.min(8, Number(symbol.width) || 2))}px`);
+    if (needsAutoContrast(symbol)) preview.classList.add("legend-symbol-auto-contrast");
     return preview;
 }
 
+export function needsAutoContrast(symbol = {}) {
+    const color = parseCssColor(symbol.color);
+    if (!color || relativeLuminance(color) < 0.82) return false;
+
+    const type = symbol.type ?? "fill";
+    if (type === "line") return true;
+
+    const outline = parseCssColor(symbol.outlineColor || symbol.color);
+    return !outline || relativeLuminance(outline) >= 0.72;
+}
+
 function resolveFirstProperty(layerId, properties) {
     for (const property of properties ?? []) {
         const selected = getSelectedLayerValue(layerId, "paint", property);
@@ -80,3 +92,25 @@ function createFallbackSymbol(type = "fill") {
         width: type === "circle" ? 5 : 2
     };
 }
+
+function parseCssColor(value) {
+    if (typeof value !== "string" || typeof document === "undefined") return null;
+    const probe = document.createElement("span");
+    probe.style.color = "";
+    probe.style.color = value;
+    if (!probe.style.color) return null;
+    document.body.append(probe);
+    const computed = getComputedStyle(probe).color;
+    probe.remove();
+    const match = computed.match(/rgba?\(\s*([\d.]+)[, ]+\s*([\d.]+)[, ]+\s*([\d.]+)/i);
+    if (!match) return null;
+    return [Number(match[1]), Number(match[2]), Number(match[3])];
+}
+
+function relativeLuminance([r, g, b]) {
+    const channels = [r, g, b].map(value => {
+        const normalized = Math.max(0, Math.min(255, value)) / 255;
+        return normalized <= 0.04045 ? normalized / 12.92 : ((normalized + 0.055) / 1.055) ** 2.4;
+    });
+    return (0.2126 * channels[0]) + (0.7152 * channels[1]) + (0.0722 * channels[2]);
+}
diff --git a/style.css b/style.css
index 50c79a5..804abc5 100644
--- a/style.css
+++ b/style.css
@@ -2690,6 +2690,16 @@ main {
     transform: rotate(45deg) scale(.72);
 }
 
+.legend-symbol-auto-contrast:not(.legend-symbol-line) {
+    box-shadow: 0 0 0 1px #7a828b;
+}
+.legend-symbol-symbol.legend-symbol-auto-contrast {
+    box-shadow: inset 0 0 0 2px var(--legend-outline), 0 0 0 1.4px #7a828b;
+}
+.legend-symbol-line.legend-symbol-auto-contrast {
+    filter: drop-shadow(0 1px 0 #7a828b) drop-shadow(0 -1px 0 #7a828b);
+}
+
 .reader-legend {
     display: grid;
     gap: 7px;
diff --git a/templates/scrollytelling/index.html b/templates/scrollytelling/index.html
index b041a3c..4ed734f 100644
--- a/templates/scrollytelling/index.html
+++ b/templates/scrollytelling/index.html
@@ -65,9 +65,10 @@
                 const opacity = Number.isFinite(Number(symbol.opacity)) ? Number(symbol.opacity) : 1;
                 const width = Math.max(1, Math.min(8, Number(symbol.width) || 2));
                 const type = symbol.type || 'fill';
+                const contrastClass = needsLegendContrast(color, outline, type) ? ' legend-symbol-auto-contrast' : '';
                 return `
                     <div class="legend-item">
-                        <span class="legend-symbol legend-symbol-${type}" style="--legend-color:${color};--legend-outline:${outline};--legend-opacity:${opacity};--legend-width:${width}px"></span>
+                        <span class="legend-symbol legend-symbol-${type}${contrastClass}" style="--legend-color:${color};--legend-outline:${outline};--legend-opacity:${opacity};--legend-width:${width}px"></span>
                         <span>${escapeLegendText(item.label || item.layerId || 'Élément')}</span>
                     </div>
                 `;
@@ -75,6 +76,34 @@
             legend.style.display = 'block';
         }
 
+        function needsLegendContrast(color, outline, type) {
+            const colorRgb = parseLegendColor(color);
+            if (!colorRgb || legendLuminance(colorRgb) < 0.82) return false;
+            if (type === 'line') return true;
+            const outlineRgb = parseLegendColor(outline || color);
+            return !outlineRgb || legendLuminance(outlineRgb) >= 0.72;
+        }
+
+        function parseLegendColor(value) {
+            const probe = document.createElement('span');
+            probe.style.color = '';
+            probe.style.color = value || '';
+            if (!probe.style.color) return null;
+            document.body.append(probe);
+            const computed = getComputedStyle(probe).color;
+            probe.remove();
+            const match = computed.match(/rgba?\(\s*([\d.]+)[, ]+\s*([\d.]+)[, ]+\s*([\d.]+)/i);
+            return match ? [Number(match[1]), Number(match[2]), Number(match[3])] : null;
+        }
+
+        function legendLuminance(rgb) {
+            const channels = rgb.map(value => {
+                const normalized = Math.max(0, Math.min(255, value)) / 255;
+                return normalized <= 0.04045 ? normalized / 12.92 : Math.pow((normalized + 0.055) / 1.055, 2.4);
+            });
+            return (0.2126 * channels[0]) + (0.7152 * channels[1]) + (0.0722 * channels[2]);
+        }
+
         function escapeLegendText(value) {
             return String(value ?? '').replace(/[&<>"']/g, char => ({
                 '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;'
diff --git a/templates/scrollytelling/style.css b/templates/scrollytelling/style.css
index 3fc69ab..72f148f 100644
--- a/templates/scrollytelling/style.css
+++ b/templates/scrollytelling/style.css
@@ -344,4 +344,7 @@ a:visited {
 .legend-symbol-line { height: 0; border-top: var(--legend-width) solid var(--legend-color); }
 .legend-symbol-circle { border-radius: 50%; background: var(--legend-color); border: 1px solid var(--legend-outline); }
 .legend-symbol-symbol { width: 12px; height: 12px; transform: rotate(45deg); background: var(--legend-color); border: 1px solid var(--legend-outline); border-radius: 2px; }
+.legend-symbol-auto-contrast:not(.legend-symbol-line) { box-shadow: 0 0 0 1px #7a828b; }
+.legend-symbol-symbol.legend-symbol-auto-contrast { box-shadow: 0 0 0 1.4px #7a828b; }
+.legend-symbol-line.legend-symbol-auto-contrast { filter: drop-shadow(0 1px 0 #7a828b) drop-shadow(0 -1px 0 #7a828b); }
 
