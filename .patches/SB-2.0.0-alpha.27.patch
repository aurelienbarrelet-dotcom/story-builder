diff --git a/README.md b/README.md
index e91665b..2525467 100644
--- a/README.md
+++ b/README.md
@@ -2,6 +2,13 @@
 
 ## Dernières modifications
 
+### SB-2.0.0-alpha.27 — 2026-07-21
+
+- Correction des contours de sélection afin qu’ils restent entièrement visibles dans les listes et galeries.
+- Mise à la largeur de la colonne des sections « Légendes actives » et « Légendes disponibles ».
+- Ajout d’un espacement autour des titres des cartes d’aperçu des images.
+- Déplacement du sélecteur Desktop/Mobile de l’espace de travail vers le panneau Vue en mode édition.
+
 ### SB-2.0.0-alpha.26 — 2026-07-21
 
 - Premier patch de refactoring JavaScript sans changement fonctionnel.
@@ -443,6 +450,7 @@ La gestion des images de chapitre est centralisée dans le panneau Enrichissemen
 
 - ✔ alpha.25 : Design System
 - ✔ alpha.26 : refactoring JavaScript
+- ✔ alpha.27 : ajustements UI des collections, légendes, images et panneau Vue
 - alpha.27 : bibliothèque d’assets
 - alpha.28 : système de widgets
 - alpha.29 : architecture des exports
diff --git a/index.html b/index.html
index a70d1bf..3a4de54 100644
--- a/index.html
+++ b/index.html
@@ -3,7 +3,7 @@
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
-    <title>Story Builder — 2.0.0-alpha.26</title>
+    <title>Story Builder — 2.0.0-alpha.27</title>
     <link href="https://api.mapbox.com/mapbox-gl-js/v3.25.0/mapbox-gl.css" rel="stylesheet">
     <script src="https://api.mapbox.com/mapbox-gl-js/v3.25.0/mapbox-gl.js"></script>
     <link rel="stylesheet" href="style.css">
@@ -92,18 +92,6 @@
                 </div>
             </div>
 
-            <button
-                id="editorDeviceToggle"
-                class="editor-device-toggle"
-                type="button"
-                role="switch"
-                aria-checked="false"
-                aria-label="Basculer entre la prévisualisation desktop et mobile"
-                data-mode="desktop"
-            >
-                <span class="editor-device-option editor-device-option-desktop" title="Desktop"><svg viewBox="0 0 24 24" aria-hidden="true"><rect x="3" y="4" width="18" height="12" rx="1.5"></rect><path d="M8 20h8M12 16v4"></path></svg><span class="sr-only">Desktop</span></span>
-                <span class="editor-device-option editor-device-option-mobile" title="Mobile"><svg viewBox="0 0 24 24" aria-hidden="true"><rect x="7" y="2.5" width="10" height="19" rx="2"></rect><path d="M10 5h4M11 18.5h2"></path></svg><span class="sr-only">Mobile</span></span>
-            </button>
         </section>
 
         
diff --git a/js/core/config.js b/js/core/config.js
index ad8a481..e1f807e 100644
--- a/js/core/config.js
+++ b/js/core/config.js
@@ -1,4 +1,4 @@
-export const APP_VERSION = "2.0.0-alpha.26";
+export const APP_VERSION = "2.0.0-alpha.27";
 export const PROJECT_FORMAT = "story-builder-project";
 export const PROJECT_FORMAT_VERSION = 1;
 export const STORAGE_KEY = "storyBuilderProjectV2Architecture1";
diff --git a/js/features/map/view-panel.js b/js/features/map/view-panel.js
index a0816af..00326b1 100644
--- a/js/features/map/view-panel.js
+++ b/js/features/map/view-panel.js
@@ -17,9 +17,6 @@ export function setupViewPanel() {
     if (initialized) return;
     initialized = true;
     document.getElementById("toggleViewPanelButton")?.addEventListener("click", togglePanel);
-    document.getElementById("editorDeviceToggle")?.addEventListener("click", () => {
-        switchViewMode(activeViewMode === "desktop" ? "mobile" : "desktop");
-    });
     on(EVENTS.MAP_CAMERA_CHANGED, camera => {
         updateLiveCameraFields(camera);
         updateCaptureButtonState(camera);
@@ -58,6 +55,20 @@ export function renderViewPanel() {
         : (getSelectedSection() === "meta" ? "Revenir à la vue du projet" : "Revenir à la vue du chapitre");
 
     container.innerHTML = `
+        <div class="view-device-switch">
+            <button
+                id="editorDeviceToggle"
+                class="editor-device-toggle"
+                type="button"
+                role="switch"
+                aria-checked="${String(activeViewMode === "mobile")}"
+                aria-label="Basculer entre la prévisualisation desktop et mobile"
+                data-mode="${activeViewMode}"
+            >
+                <span class="editor-device-option editor-device-option-desktop" title="Desktop"><svg viewBox="0 0 24 24" aria-hidden="true"><rect x="3" y="4" width="18" height="12" rx="1.5"></rect><path d="M8 20h8M12 16v4"></path></svg><span class="sr-only">Desktop</span></span>
+                <span class="editor-device-option editor-device-option-mobile" title="Mobile"><svg viewBox="0 0 24 24" aria-hidden="true"><rect x="7" y="2.5" width="10" height="19" rx="2"></rect><path d="M10 5h4M11 18.5h2"></path></svg><span class="sr-only">Mobile</span></span>
+            </button>
+        </div>
         <p class="view-device-status ${activeViewMode === "mobile" && !hasMobileView ? "is-inherited" : ""}">${activeViewMode === "desktop" ? "Vue de référence" : mobileStatus}</p>
         <form id="cameraForm" class="camera-form view-camera-form">
             <div class="camera-field"><label for="cameraLongitudeInput">Longitude</label><input id="cameraLongitudeInput" data-camera-field type="number" min="-180" max="180" step="0.000001" value="${Number(camera?.center?.[0] ?? 0).toFixed(6)}"></div>
@@ -74,6 +85,9 @@ export function renderViewPanel() {
         <p class="property-help view-help">${activeViewMode === "mobile" ? "La vue mobile suit la vue desktop tant qu’aucun cadrage mobile n’est enregistré." : "La vue desktop est toujours la vue de référence."}</p>`;
 
     bindMapEditorPanelEvents();
+    document.getElementById("editorDeviceToggle")?.addEventListener("click", () => {
+        switchViewMode(activeViewMode === "desktop" ? "mobile" : "desktop");
+    });
     container.querySelectorAll("[data-view-mode]").forEach(button => {
         button.addEventListener("click", () => switchViewMode(button.dataset.viewMode));
     });
diff --git a/style.css b/style.css
index 9429250..792ca9e 100644
--- a/style.css
+++ b/style.css
@@ -5010,7 +5010,7 @@ body:not(.reader-mode) .chapter {
     --ui-field-height: 34px;
     --ui-transition: border-color .14s ease, background-color .14s ease, box-shadow .14s ease, color .14s ease;
     --ui-focus-ring: 0 0 0 3px rgba(130, 11, 28, .16);
-    --ui-selection-ring: 0 0 0 2px var(--ui-accent);
+    --ui-selection-ring: inset 0 0 0 var(--border-width) var(--ui-accent);
 }
 
 /* Tous les contrôles de saisie partagent le même contrat visuel. */
@@ -5340,3 +5340,32 @@ body:not(.reader-mode) .chapter:hover {
 .legend-selection-bar { margin-top: var(--space-2); }
 .legend-add-selected,
 .legend-icon-button { display: none !important; }
+
+
+/* Alpha 27 — ajustements UI des collections et de la vue */
+.view-device-switch {
+    display: flex;
+    justify-content: center;
+    width: 100%;
+    margin-bottom: var(--space-5);
+}
+
+.view-panel-content .editor-device-toggle {
+    justify-self: auto;
+    box-shadow: 0 var(--space-2) var(--space-6) rgba(29, 35, 42, .12);
+}
+
+.legend-panel-content .legend-section,
+.legend-current-list,
+.legend-available-list,
+.legend-item-card,
+.legend-available-row {
+    box-sizing: border-box;
+    width: 100%;
+    min-width: 0;
+}
+
+.asset-name-static {
+    box-sizing: border-box;
+    padding: var(--space-2) var(--space-3) 0;
+}
