diff --git a/README.md b/README.md
index 12cdb98..d2a5444 100644
--- a/README.md
+++ b/README.md
@@ -1,4 +1,4 @@
-## SB-2.0.0-alpha.34.2.1
+## SB-2.0.0-alpha.34.2.2
 
 ### Alpha 33.4 — contraste automatique des légendes
 
@@ -643,3 +643,11 @@ La gestion des images de chapitre est centralisée dans le panneau Enrichissemen
 - Ajout d’une entrée **Modèles 3D** dans le rail des panneaux de droite.
 - Ajout d’un panneau vide dédié, sans modification du format de projet ni de la carte Mapbox.
 - Cette étape prépare l’interface d’import GLB prévue pour la version suivante.
+
+
+### 2.0.0-alpha.34.2.2
+
+- Ajout du bouton **Importer un modèle GLB** dans le panneau Modèles 3D.
+- Validation du format `.glb`, du fichier vide et d’une limite temporaire de 100 Mo.
+- Affichage du nom, de la taille, du type MIME et de l’état du fichier sélectionné.
+- Le modèle reste uniquement en mémoire : il n’est pas encore enregistré dans le JSON du projet.
diff --git a/index.html b/index.html
index 73de651..3ce9e7f 100644
--- a/index.html
+++ b/index.html
@@ -3,7 +3,7 @@
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
-    <title>Story Builder — 2.0.0-alpha.34.2.1</title>
+    <title>Story Builder — 2.0.0-alpha.34.2.2</title>
     <link href="https://api.mapbox.com/mapbox-gl-js/v3.25.0/mapbox-gl.css" rel="stylesheet">
     <script src="https://api.mapbox.com/mapbox-gl-js/v3.25.0/mapbox-gl.js"></script>
     <link rel="stylesheet" href="style.css">
@@ -213,8 +213,13 @@
                     <h2>Modèles 3D</h2>
                 </div>
             </header>
-            <div class="properties-panel-content">
-                <p class="panel-empty-state">Le module d’import sera ajouté dans la prochaine étape.</p>
+            <div class="properties-panel-content models3d-panel-content">
+                <button id="importModel3dButton" class="ui-button ui-button--primary models3d-import-button" type="button">Importer un modèle GLB</button>
+                <input id="model3dFileInput" type="file" accept=".glb,model/gltf-binary" hidden>
+                <p class="models3d-help">Le fichier est lu temporairement en mémoire. Il ne sera pas encore ajouté à la sauvegarde du projet.</p>
+                <div id="model3dSelection" class="models3d-selection" aria-live="polite">
+                    <p class="panel-empty-state">Aucun modèle sélectionné.</p>
+                </div>
             </div>
         </aside>
 
diff --git a/js/app.js b/js/app.js
index f6e295a..01d9d73 100644
--- a/js/app.js
+++ b/js/app.js
@@ -40,6 +40,7 @@ import { exportPublication } from "./core/export-service.js";
 import { copySelectedChapters, duplicateSelectedChapters, hasChapterClipboard, pasteChapters } from "./features/chapters/chapter-clipboard.js";
 import { renderAssetsPanel, setupAssetsPanel } from "./features/assets/assets-panel.js";
 import { setupMotionManager } from "./features/motion/motion-manager.js";
+import { setupModels3dPanel } from "./features/models3d/models3d-panel.js";
 
 const ELEMENT_IDS = Object.freeze({
     addChapterButton: "addChapterButton",
@@ -234,6 +235,7 @@ function startApplication() {
     setupRightPanels();
     setupLegendPanel();
     setupAssetsPanel();
+    setupModels3dPanel();
     setupChapterStyleClipboard();
     setupProductivityShortcuts();
     setupReaderMode();
diff --git a/js/core/config.js b/js/core/config.js
index b8f9804..15d0558 100644
--- a/js/core/config.js
+++ b/js/core/config.js
@@ -1,4 +1,4 @@
-export const APP_VERSION = "2.0.0-alpha.34.2.1";
+export const APP_VERSION = "2.0.0-alpha.34.2.2";
 export const PROJECT_FORMAT = "story-builder-project";
 export const PROJECT_FORMAT_VERSION = 1;
 export const STORAGE_KEY = "storyBuilderProjectV2Architecture1";
diff --git a/js/features/models3d/models3d-panel.js b/js/features/models3d/models3d-panel.js
new file mode 100644
index 0000000..677c7a2
--- /dev/null
+++ b/js/features/models3d/models3d-panel.js
@@ -0,0 +1,91 @@
+const MAX_PREVIEW_SIZE_BYTES = 100 * 1024 * 1024;
+
+let selectedModelFile = null;
+
+export function setupModels3dPanel() {
+    const importButton = document.getElementById("importModel3dButton");
+    const fileInput = document.getElementById("model3dFileInput");
+
+    if (!importButton || !fileInput) return;
+
+    importButton.addEventListener("click", () => fileInput.click());
+    fileInput.addEventListener("change", async () => {
+        const file = fileInput.files?.[0];
+        fileInput.value = "";
+        if (!file) return;
+
+        try {
+            validateGlbFile(file);
+            await file.arrayBuffer();
+            selectedModelFile = file;
+            renderSelection();
+        } catch (error) {
+            selectedModelFile = null;
+            renderSelection(error instanceof Error ? error.message : "Impossible de lire ce fichier.");
+        }
+    });
+}
+
+function validateGlbFile(file) {
+    const isGlb = file.name.toLowerCase().endsWith(".glb");
+    if (!isGlb) {
+        throw new Error("Sélectionnez un fichier au format GLB.");
+    }
+    if (file.size === 0) {
+        throw new Error("Le fichier sélectionné est vide.");
+    }
+    if (file.size > MAX_PREVIEW_SIZE_BYTES) {
+        throw new Error("Le fichier dépasse la limite temporaire de 100 Mo.");
+    }
+}
+
+function renderSelection(errorMessage = "") {
+    const container = document.getElementById("model3dSelection");
+    if (!container) return;
+
+    container.replaceChildren();
+
+    if (errorMessage) {
+        const message = document.createElement("p");
+        message.className = "models3d-message models3d-message--error";
+        message.textContent = errorMessage;
+        container.append(message);
+        return;
+    }
+
+    if (!selectedModelFile) {
+        const empty = document.createElement("p");
+        empty.className = "panel-empty-state";
+        empty.textContent = "Aucun modèle sélectionné.";
+        container.append(empty);
+        return;
+    }
+
+    const details = document.createElement("dl");
+    details.className = "models3d-file-details";
+    appendDetail(details, "Nom", selectedModelFile.name);
+    appendDetail(details, "Taille", formatFileSize(selectedModelFile.size));
+    appendDetail(details, "Type", selectedModelFile.type || "model/gltf-binary");
+    appendDetail(details, "État", "Fichier chargé en mémoire");
+    container.append(details);
+}
+
+function appendDetail(list, label, value) {
+    const term = document.createElement("dt");
+    term.textContent = label;
+    const description = document.createElement("dd");
+    description.textContent = value;
+    list.append(term, description);
+}
+
+function formatFileSize(bytes) {
+    if (bytes < 1024) return `${bytes} octet${bytes > 1 ? "s" : ""}`;
+    const units = ["Ko", "Mo", "Go"];
+    let value = bytes / 1024;
+    let unitIndex = 0;
+    while (value >= 1024 && unitIndex < units.length - 1) {
+        value /= 1024;
+        unitIndex += 1;
+    }
+    return `${value.toLocaleString("fr-FR", { maximumFractionDigits: 2 })} ${units[unitIndex]}`;
+}
diff --git a/style.css b/style.css
index 81f7546..102e673 100644
--- a/style.css
+++ b/style.css
@@ -5837,3 +5837,15 @@ body > .collection-menu { z-index: 1000; }
     .layer-picker-actions { display: grid; grid-template-columns: 1fr 1fr; }
     .layer-picker-actions .button { width: 100%; min-height: 40px; }
 }
+
+
+/* Alpha 34.2.2 — sélection temporaire d’un modèle GLB */
+.models3d-panel-content { display: grid; align-content: start; gap: var(--space-5); }
+.models3d-import-button { width: 100%; }
+.models3d-help { margin: 0; color: var(--text-secondary, #667085); font-size: 12px; line-height: 1.5; }
+.models3d-selection { min-width: 0; }
+.models3d-file-details { display: grid; grid-template-columns: minmax(72px, auto) minmax(0, 1fr); gap: var(--space-3) var(--space-5); margin: 0; padding: var(--space-5); border: var(--border-width) solid var(--panel-border, #d4d7da); border-radius: var(--control-radius); background: var(--color-surface, #f4f5f6); }
+.models3d-file-details dt { font-size: 12px; font-weight: 700; color: var(--text-secondary, #667085); }
+.models3d-file-details dd { min-width: 0; margin: 0; overflow-wrap: anywhere; font-size: 12px; color: var(--text-primary, #1d2939); }
+.models3d-message { margin: 0; padding: var(--space-4); border-radius: var(--control-radius); font-size: 12px; line-height: 1.45; }
+.models3d-message--error { border: var(--border-width) solid var(--danger, #d92d20); color: var(--danger, #b42318); background: #fff1f0; }
