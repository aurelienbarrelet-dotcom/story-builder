diff --git a/README.md b/README.md
index 53b25b0..1adf18b 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,8 @@
-## SB-2.0.0-alpha.34.3.10
+## SB-2.0.0-alpha.34.3.11
 
-### 2.0.0-alpha.34.3.10
+### 2.0.0-alpha.34.3.11
+
+- Ajout d’une liste recherchable des objets 3D placés.
 
 - Ajout de la suppression, de la visibilité et du verrouillage des instances.
 
diff --git a/index.html b/index.html
index 6c7739c..f8eae78 100644
--- a/index.html
+++ b/index.html
@@ -3,7 +3,7 @@
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
-    <title>Story Builder — 2.0.0-alpha.34.3.10</title>
+    <title>Story Builder — 2.0.0-alpha.34.3.11</title>
     <link href="https://api.mapbox.com/mapbox-gl-js/v3.25.0/mapbox-gl.css" rel="stylesheet">
     <script src="https://api.mapbox.com/mapbox-gl-js/v3.25.0/mapbox-gl.js"></script>
     <link rel="stylesheet" href="style.css">
diff --git a/js/core/config.js b/js/core/config.js
index 19bff9a..c61359e 100644
--- a/js/core/config.js
+++ b/js/core/config.js
@@ -1,4 +1,4 @@
-export const APP_VERSION = "2.0.0-alpha.34.3.10";
+export const APP_VERSION = "2.0.0-alpha.34.3.11";
 export const PROJECT_FORMAT = "story-builder-project";
 export const PROJECT_FORMAT_VERSION = 1;
 export const STORAGE_KEY = "storyBuilderProjectV2Architecture1";
diff --git a/js/features/models3d/models3d-instance-panel.js b/js/features/models3d/models3d-instance-panel.js
index 49faa18..79ff568 100644
--- a/js/features/models3d/models3d-instance-panel.js
+++ b/js/features/models3d/models3d-instance-panel.js
@@ -17,7 +17,37 @@ export function renderInstanceEditor() {
     if (!container) return;
     container.replaceChildren();
     const project = getProject();
-    const instance = project?.models3dInstances?.find(item => item.id === getSelectedModelInstanceId());
+    const instances = Array.isArray(project?.models3dInstances) ? project.models3dInstances : [];
+    const browser = document.createElement("section");
+    browser.className = "models3d-object-browser";
+    const browserTitle = document.createElement("h3");
+    browserTitle.textContent = `Objets placés (${instances.length})`;
+    const search = document.createElement("input");
+    search.type = "search";
+    search.placeholder = "Rechercher un objet";
+    const list = document.createElement("div");
+    list.className = "models3d-object-list";
+    const renderList = () => {
+        list.replaceChildren();
+        const query = search.value.trim().toLowerCase();
+        instances.forEach((item, index) => {
+            const source = project?.assets?.models?.find(model => model.id === item.modelId);
+            const name = item.name || source?.name || `Objet ${index + 1}`;
+            if (query && !name.toLowerCase().includes(query)) return;
+            const button = document.createElement("button");
+            button.type = "button";
+            button.className = "models3d-object-row";
+            button.classList.toggle("is-selected", item.id === getSelectedModelInstanceId());
+            button.textContent = `${item.visible === false ? "○" : "●"} ${name}`;
+            button.addEventListener("click", () => selectModelInstance(item.id));
+            list.append(button);
+        });
+    };
+    search.addEventListener("input", renderList);
+    browser.append(browserTitle, search, list);
+    container.append(browser);
+    renderList();
+    const instance = instances.find(item => item.id === getSelectedModelInstanceId());
     if (!instance) return;
     const model = project?.assets?.models?.find(item => item.id === instance.modelId);
 
diff --git a/style.css b/style.css
index f68ac5f..298b278 100644
--- a/style.css
+++ b/style.css
@@ -5895,3 +5895,9 @@ body > .collection-menu { z-index: 1000; }
 .models3d-animation-editor { grid-template-columns: 1fr; }
 
 .models3d-state-editor { grid-template-columns: 1fr 1fr; }
+
+.models3d-object-browser { display:grid; gap:var(--space-3); margin-bottom:var(--space-4); }
+.models3d-object-browser h3 { margin:0; font-size:13px; }
+.models3d-object-list { display:grid; gap:var(--space-1); max-height:180px; overflow:auto; }
+.models3d-object-row { width:100%; padding:8px 10px; border:1px solid var(--panel-border,#d4d7da); border-radius:6px; background:white; text-align:left; cursor:pointer; }
+.models3d-object-row.is-selected { border-color:var(--accent-color,#2563eb); background:color-mix(in srgb,var(--accent-color,#2563eb) 8%,white); }
