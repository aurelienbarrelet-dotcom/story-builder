diff --git a/index.html b/index.html
index 33c90c0..cc1ee97 100644
--- a/index.html
+++ b/index.html
@@ -201,8 +201,12 @@
         <aside id="assetsPanel" class="assets-panel collection-panel" data-collection="assets">
             <header class="assets-panel-header collection-panel-header">
                 <h2>Enrichissements</h2>
-                <button id="importAssetsButton" class="ui-icon-button ui-icon-button--accent collection-add-button assets-import-button" type="button" aria-label="Importer des images" title="Importer des images">+</button>
+                <div class="assets-import-actions" aria-label="Importer un enrichissement">
+                    <button id="importAssetsButton" class="ui-icon-button ui-icon-button--accent collection-add-button assets-import-button" type="button" aria-label="Importer des images" title="Importer des images">+</button>
+                    <button id="importModel3dButton" class="ui-icon-button ui-icon-button--accent collection-add-button assets-import-button assets-import-button--model" type="button" aria-label="Importer des modèles 3D" title="Importer des modèles 3D">3D</button>
+                </div>
                 <input id="assetsFileInput" type="file" accept="image/*" multiple hidden>
+                <input id="model3dFileInput" type="file" accept=".glb,model/gltf-binary" multiple hidden>
             </header>
             <div id="assetsPanelContent" class="assets-panel-content"></div>
         </aside>
@@ -214,16 +218,8 @@
                 </div>
             </header>
             <div class="properties-panel-content models3d-panel-content">
-                <div id="model3dDropZone" class="models3d-drop-zone">
-                    <button id="importModel3dButton" class="ui-button ui-button--primary models3d-import-button" type="button">Importer des modèles GLB</button>
-                    <input id="model3dFileInput" type="file" accept=".glb,model/gltf-binary" multiple hidden>
-                    <p class="models3d-drop-zone-label">ou déposez vos fichiers .glb ici</p>
-                </div>
-                <p class="models3d-help">Les modèles importés sont intégrés à la sauvegarde du projet et affichés ci-dessous.</p>
+                <p class="models3d-help">Sélectionnez un objet placé sur la carte pour modifier sa position, son état et ses transformations.</p>
                 <div id="model3dInstanceEditor" class="models3d-instance-editor" aria-live="polite"></div>
-                <div id="model3dSelection" class="models3d-selection" aria-live="polite">
-                    <p class="panel-empty-state">Aucun modèle sélectionné.</p>
-                </div>
             </div>
         </aside>
 
diff --git a/js/features/assets/assets-panel.js b/js/features/assets/assets-panel.js
index 7ca10b4..8c4cf90 100644
--- a/js/features/assets/assets-panel.js
+++ b/js/features/assets/assets-panel.js
@@ -2,6 +2,7 @@ import { MAX_IMAGE_SIZE } from "../../core/config.js";
 import { commitProjectChange } from "../../core/project-service.js";
 import { getProject, getSelectedChapter } from "../../core/store.js";
 import { readFileAsDataUrl } from "../../core/utils.js";
+import { renderModels } from "../models3d/models3d-panel.js";
 import {
     createCollectionSelection,
     renderCollectionSelectionBar
@@ -136,17 +137,27 @@ export function renderAssetsPanel() {
         </section>`;
 
     container.innerHTML = `
-        <div class="assets-workspace">
-            <div class="assets-gallery-column">
-                <div class="assets-gallery" aria-label="Galerie d’images">
-                    ${galleryMarkup}
+        <section class="assets-library-section" aria-labelledby="assetsImagesTitle">
+            <h3 id="assetsImagesTitle" class="assets-library-title">Images</h3>
+            <div class="assets-workspace">
+                <div class="assets-gallery-column">
+                    <div class="assets-gallery" aria-label="Galerie d’images">
+                        ${galleryMarkup}
+                    </div>
+                    <div class="collection-selection-bar assets-selection-bar" aria-live="polite" hidden></div>
                 </div>
-                <div class="collection-selection-bar assets-selection-bar" aria-live="polite" hidden></div>
+                <aside class="asset-inspector" aria-label="Propriétés de l’image sélectionnée">
+                    ${inspectorMarkup}
+                </aside>
             </div>
-            <aside class="asset-inspector" aria-label="Propriétés de l’image sélectionnée">
-                ${inspectorMarkup}
-            </aside>
-        </div>`;
+        </section>
+        <section class="assets-library-section assets-model-library" aria-labelledby="assetsModelsTitle">
+            <h3 id="assetsModelsTitle" class="assets-library-title">Modèles 3D</h3>
+            <p class="models3d-help">Les modèles GLB sont des ressources du projet. Vous pouvez les prévisualiser, les renommer, les dupliquer, les supprimer ou les placer sur la carte.</p>
+            <div id="assetsModel3dLibrary" class="models3d-selection" aria-live="polite"></div>
+        </section>`;
+
+    renderModels();
 
     container.querySelectorAll("[data-select-asset]").forEach(card => {
         card.addEventListener("click", event => {
diff --git a/js/features/models3d/models3d-panel.js b/js/features/models3d/models3d-panel.js
index a9960cc..f5b3795 100644
--- a/js/features/models3d/models3d-panel.js
+++ b/js/features/models3d/models3d-panel.js
@@ -11,8 +11,6 @@ export function setupModels3dPanel() {
     const fileInput = document.getElementById("model3dFileInput");
     if (!importButton || !fileInput) return;
 
-    const dropZone = document.getElementById("model3dDropZone");
-
     importButton.addEventListener("click", () => fileInput.click());
     fileInput.addEventListener("change", async () => {
         const files = [...(fileInput.files ?? [])];
@@ -20,27 +18,6 @@ export function setupModels3dPanel() {
         await importGlbFiles(files);
     });
 
-    if (dropZone) {
-        ["dragenter", "dragover"].forEach(eventName => {
-            dropZone.addEventListener(eventName, event => {
-                event.preventDefault();
-                if (!containsFiles(event)) return;
-                dropZone.classList.add("models3d-drop-zone--active");
-                if (event.dataTransfer) event.dataTransfer.dropEffect = "copy";
-            });
-        });
-        ["dragleave", "drop"].forEach(eventName => {
-            dropZone.addEventListener(eventName, event => {
-                event.preventDefault();
-                dropZone.classList.remove("models3d-drop-zone--active");
-            });
-        });
-        dropZone.addEventListener("drop", async event => {
-            if (!containsFiles(event)) return;
-            await importGlbFiles([...(event.dataTransfer?.files ?? [])]);
-        });
-    }
-
     on(EVENTS.PROJECT_REPLACED, () => renderModels());
     on(EVENTS.MODEL3D_PLACEMENT_CHANGED, ({ active, modelId }) => updatePlacementButtons(active, modelId));
     renderModels();
@@ -70,10 +47,6 @@ async function importGlbFiles(files) {
     }
 }
 
-function containsFiles(event) {
-    return [...(event.dataTransfer?.types ?? [])].includes("Files");
-}
-
 function getModelLibrary() {
     const project = getProject();
     if (!project) return [];
@@ -100,9 +73,9 @@ function readFileAsBase64(file) {
     });
 }
 
-function renderModels(errorMessage = "") {
+export function renderModels(errorMessage = "") {
     releasePreviewUrls();
-    const container = document.getElementById("model3dSelection");
+    const container = document.getElementById("assetsModel3dLibrary");
     if (!container) return;
     container.replaceChildren();
     if (errorMessage) {
diff --git a/style.css b/style.css
index f57b7a6..e4fbe46 100644
--- a/style.css
+++ b/style.css
@@ -5901,3 +5901,29 @@ body > .collection-menu { z-index: 1000; }
 .models3d-object-list { display:grid; gap:var(--space-1); max-height:180px; overflow:auto; }
 .models3d-object-row { width:100%; padding:8px 10px; border:1px solid var(--panel-border,#d4d7da); border-radius:6px; background:white; text-align:left; cursor:pointer; }
 .models3d-object-row.is-selected { border-color:var(--accent-color,#2563eb); background:color-mix(in srgb,var(--accent-color,#2563eb) 8%,white); }
+
+/* Enrichissements : bibliothèque de ressources */
+.assets-import-actions {
+    display: flex;
+    align-items: center;
+    gap: 0.4rem;
+}
+
+.assets-import-button--model {
+    width: auto;
+    min-width: 2.35rem;
+    padding-inline: 0.45rem;
+    font-size: 0.7rem;
+    font-weight: 800;
+}
+
+.assets-library-section + .assets-library-section {
+    margin-top: 1.25rem;
+    padding-top: 1.25rem;
+    border-top: 1px solid var(--border-color, rgba(127, 127, 127, 0.25));
+}
+
+.assets-library-title {
+    margin: 0 0 0.75rem;
+    font-size: 0.95rem;
+}
